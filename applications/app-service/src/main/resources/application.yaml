server:
  port: 8080
spring:
  application:
    name: "CrediYaAuth"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
adapters:
  r2dbc:
    host: "aws-1-sa-east-1.pooler.supabase.com"
    port: 5432
    database: "postgres"
    schema: "public"
    username: "postgres.zoesbxsbinmyetggudtc"
    password: "Zednumyer123"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
